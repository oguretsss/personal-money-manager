services:
  api:
    build: ./api
    env_file: .env
    volumes:
      - ./data:/app/data
    ports:
      - "8001:8001"
    restart: unless-stopped

  bot:
    build: ./bot
    env_file: .env
    depends_on:
      - api
    restart: unless-stopped
